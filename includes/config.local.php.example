<?php
// ============================================================
// SignalTrace Local Configuration
// Copy this file to config.local.php and fill in your values.
// Never commit config.local.php to version control.
// ============================================================

// Admin credentials
// Generate password hash: php -r "echo password_hash('yourpassword', PASSWORD_DEFAULT);"
define('ADMIN_USERNAME',      'admin');
define('ADMIN_PASSWORD_HASH', '$2y$12$...');

// Visitor hash salt — generate with: openssl rand -hex 64
define('VISITOR_HASH_SALT', '');

// Base URL of your SignalTrace install (no trailing slash)
// Used for feed and export URL display in Settings.
// define('BASE_URL', 'https://yourdomain.example');

// Trusted reverse proxy IP — only set if running behind a known proxy.
// SignalTrace will trust X-Forwarded-For only from this IP.
// define('TRUSTED_PROXY_IP', '');

// Self-referrer domain — requests from this domain to the root path
// receive a score penalty. Set to your own domain.
// define('SELF_REFERER_DOMAIN', 'yourdomain.example');

// Export API token for Splunk/Grafana/automation integrations.
// Generate with: openssl rand -hex 32
// define('EXPORT_API_TOKEN', '');

// Admin login rate limiting
// define('AUTH_MAX_FAILURES', 5);
// define('AUTH_LOCKOUT_SECS', 900);

// Demo mode — displays the demo banner on admin pages.
// The reset mechanism is separate and not included in the repository.
// define('DEMO_MODE', true);
// define('DEMO_ADMIN_USERNAME', 'demo');
// define('DEMO_ADMIN_PASSWORD', 'trysignaltrace');

// ============================================================
// Cloudflare Access (optional)
// Adds an identity layer in front of /admin using Cloudflare
// Zero Trust. When disabled (default), Basic Auth is used alone.
// See: https://github.com/veddegre/signaltrace/wiki/Cloudflare-Access
// ============================================================
// define('CF_ACCESS_ENABLED',     true);
// define('CF_ACCESS_AUD',         'your-application-audience-tag');
// define('CF_ACCESS_TEAM_DOMAIN', 'yourteam.cloudflareaccess.com');
